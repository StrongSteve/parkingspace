# Render.io configuration
services:
  - type: web
    name: parking-monitor
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8000
      - key: PIXEL_THRESHOLD
        value: 0.15
      - key: TF_CONFIDENCE
        value: 0.5
      - key: TF_ENABLED
        value: "true"
      - key: TF_PRELOAD
        value: "false"  # Don't preload on startup to save memory
