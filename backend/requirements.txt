fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
Pillow==10.2.0
numpy==1.26.3
opencv-python-headless==4.9.0.80
psutil==5.9.8
tflite-runtime==2.14.0

# Detection backends (both run in parallel for comparison):
# - OpenCV DNN (MobileNet-SSD ~23MB model, uses ~30MB RAM)
# - TFLite (SSD MobileNet V1 ~4MB model, uses ~20MB RAM)
